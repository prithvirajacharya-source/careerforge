import type { Metadata } from "next"; import SiteHeader from "@/components/SiteHeader"; import CareerSwitchGate from "@/components/user/CareerSwitchGate"; import { careerProfiles } from "@/lib/careerProfiles"; import { getCareerCountryProfiles } from "@/lib/careerCountryProfiles";
export const metadata: Metadata = { title: "Career Switch Planner | SEKUR", robots: { index: false, follow: false } }; const supported = ["mechanical-engineer","cybersecurity-analyst","software-engineer","electrical-engineer","data-scientist","registered-nurse","accountant"];
export default function Page() { const careers=Object.values(careerProfiles).filter(c=>supported.includes(c.slug)); const markets=careers.flatMap(c=>getCareerCountryProfiles(c.slug)); return <main className="sekur-intelligence min-h-screen text-white"><SiteHeader/><section className="mx-auto max-w-7xl px-5 py-14"><CareerSwitchGate careers={careers} markets={markets}/></section></main>; }
