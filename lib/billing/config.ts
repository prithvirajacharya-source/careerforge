export type BillingConfig = { configured: boolean; secretKey: string | null; webhookSecret: string | null; proPriceId: string | null; appUrl: string };
export function getBillingConfig(): BillingConfig { const secretKey = process.env.STRIPE_SECRET_KEY ?? null; const webhookSecret = process.env.STRIPE_WEBHOOK_SECRET ?? null; const proPriceId = process.env.STRIPE_PRO_PRICE_ID ?? null; const rawAppUrl = process.env.NEXT_PUBLIC_APP_URL ?? "http://localhost:3000"; let appUrl = ""; try { const parsed = new URL(rawAppUrl); if (parsed.protocol === "https:" || parsed.protocol === "http:") appUrl = parsed.origin; } catch { /* Invalid configuration fails closed below. */ } return { configured: Boolean(secretKey && proPriceId && appUrl), secretKey, webhookSecret, proPriceId, appUrl }; }
