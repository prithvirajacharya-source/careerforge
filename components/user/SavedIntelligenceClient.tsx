"use client";
import Link from "next/link"; import type {User} from "@supabase/supabase-js"; import {useEffect,useState} from "react"; import {supabase} from "@/lib/supabase"; import AlertPreferencesControl from "./AlertPreferencesControl";
type SavedItem={id:number;item_type:"career"|"country"|"career_market";career_slug:string|null;country_slug:string|null;favorite:boolean};type SavedComparison={id:number;name:string;targets:Array<{countrySlug?:string;careerSlug?:string}>};
export default function SavedIntelligenceClient({user,names}:{user:User;names:Record<string,string>}){const[items,setItems]=useState<SavedItem[]>([]);const[comparisons,setComparisons]=useState<SavedComparison[]>([]);const[loading,setLoading]=useState(true);const[message,setMessage]=useState("");
useEffect(()=>{let active=true;Promise.all([supabase.from("saved_career_markets").select("*").eq("user_id",user.id).order("updated_at",{ascending:false}),supabase.from("saved_career_comparisons").select("*").eq("user_id",user.id).order("updated_at",{ascending:false})]).then(([saved,rows])=>{if(!active)return;if(saved.error||rows.error)setMessage(saved.error?.message??rows.error?.message??"Could not load saved intelligence.");setItems((saved.data??[])as SavedItem[]);setComparisons((rows.data??[])as SavedComparison[]);setLoading(false)});return()=>{active=false}},[user.id]);
async function removeItem(id:number){await supabase.from("saved_career_markets").delete().eq("id",id).eq("user_id",user.id);setItems(items.filter(x=>x.id!==id))}async function favorite(item:SavedItem){const next=!item.favorite;const{error}=await supabase.from("saved_career_markets").update({favorite:next,updated_at:new Date().toISOString()}).eq("id",item.id).eq("user_id",user.id);if(!error)setItems(items.map(x=>x.id===item.id?{...x,favorite:next}:x))}async function removeComparison(id:number){await supabase.from("saved_career_comparisons").delete().eq("id",id).eq("user_id",user.id);setComparisons(comparisons.filter(x=>x.id!==id))}
if(loading)return <div className="glass-panel rounded-2xl border p-8">Loading saved intelligence...</div>;const href=(x:SavedItem)=>x.item_type==="career"?`/careers/${x.career_slug}`:x.item_type==="country"?`/countries/${x.country_slug}`:`/careers/${x.career_slug}?country=${x.country_slug}`;const title=(x:SavedItem)=>x.item_type==="career"?names[x.career_slug??""]:x.item_type==="country"?names[x.country_slug??""]:`${names[x.career_slug??""]} · ${names[x.country_slug??""]}`;
return <div><div className="flex flex-wrap items-end justify-between gap-4"><div><h1 className="text-4xl font-black">Saved intelligence</h1><p className="mt-3 text-slate-400">Your private career, country and market watchlist.</p></div><div className="flex gap-2"><Link href="/career-switch" className="rounded-xl border border-white/15 px-4 py-3 font-bold">Switch planner</Link><Link href="/opportunity-report" className="rounded-xl bg-emerald-300 px-4 py-3 font-black text-slate-950">Build report</Link></div></div>{message&&<div className="mt-5 rounded-xl border border-red-300/20 p-4 text-sm">{message}</div>}<section className="mt-8"><h2 className="text-xl font-black">Careers and markets</h2>{items.length?<div className="mt-4 grid gap-4 md:grid-cols-2 xl:grid-cols-3">{items.map(item=><article key={item.id} className="glass-card rounded-2xl border p-5"><div className="flex justify-between gap-3"><div><div className="text-xs font-black uppercase text-emerald-300">{item.item_type.replace("_"," ")}</div><Link href={href(item)} className="mt-2 block text-lg font-black">{title(item)||"Saved intelligence"}</Link></div><button onClick={()=>favorite(item)} aria-pressed={item.favorite} aria-label="Toggle favorite">{item.favorite?"★":"☆"}</button></div><div className="mt-5 flex flex-wrap items-center gap-3"><button onClick={()=>removeItem(item.id)} className="text-xs font-bold text-slate-400">Unsave</button>{item.item_type==="career_market"&&item.career_slug&&item.country_slug&&<AlertPreferencesControl careerSlug={item.career_slug} countrySlug={item.country_slug}/>}</div></article>)}</div>:<div className="glass-subtle mt-4 rounded-2xl border p-8 text-center"><h3 className="font-black">Nothing saved yet</h3><Link href="/careers" className="mt-4 inline-block text-emerald-300">Explore careers</Link></div>}</section><section className="mt-10"><h2 className="text-xl font-black">Saved comparisons</h2>{comparisons.length?<div className="mt-4 grid gap-4 md:grid-cols-2">{comparisons.map(c=><article key={c.id} className="glass-subtle rounded-2xl border p-5"><h3 className="font-black">{c.name}</h3><div className="mt-4 flex gap-4"><Link href={`/compare?left=${c.targets[0]?.countrySlug}&right=${c.targets[1]?.countrySlug}`} className="text-emerald-300">Open comparison</Link><button onClick={()=>removeComparison(c.id)} className="text-slate-400">Remove</button></div></article>)}</div>:<p className="mt-4 text-slate-500">No saved comparisons.</p>}</section></div>}
