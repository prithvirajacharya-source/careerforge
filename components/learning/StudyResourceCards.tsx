import type { StudyRecommendation } from "@/lib/learning/types";
export default function StudyResourceCards({ recommendations }: { recommendations: StudyRecommendation[] }) {
  if (!recommendations.length) return <p className="mt-5 text-slate-400">Verified learning recommendations are still being mapped for this skill set.</p>;
  return <div className="mt-6 divide-y divide-slate-200 border-y border-slate-200">{recommendations.map(({ resource, matchedSkills, reason }) => <article key={resource.id} className="py-5"><div className="flex justify-between gap-3"><span className="text-xs font-bold uppercase tracking-wide text-blue-700">{resource.provider}</span><span className="text-xs capitalize text-slate-600">{resource.pricing}</span></div><h3 className="mt-2 text-lg font-bold">{resource.title}</h3><p className="mt-2 text-sm leading-6 text-slate-600">{resource.description}</p><p className="mt-3 text-xs text-slate-600"><strong>Matched skills:</strong> {matchedSkills.join(", ")}</p><p className="mt-2 text-xs text-slate-600"><strong>Why SEKUR recommends this:</strong> {reason}</p><a href={resource.affiliateUrl ?? resource.url} target="_blank" rel="noopener noreferrer" className="mt-4 inline-block font-bold text-blue-700">Open resource →</a>{(resource.sponsored || resource.affiliateUrl) && <p className="mt-2 text-xs text-slate-600">Sponsored or affiliate link</p>}</article>)}</div>;
}
