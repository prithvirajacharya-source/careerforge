import type { StudyRecommendation } from "@/lib/learning/types";
export default function StudyResourceCards({ recommendations }: { recommendations: StudyRecommendation[] }) {
  if (!recommendations.length) return <p className="mt-5 text-slate-400">Verified learning recommendations are still being mapped for this skill set.</p>;
  return <div className="mt-6 grid gap-4 md:grid-cols-2 lg:grid-cols-3">{recommendations.map(({ resource, matchedSkills, reason }) => <article key={resource.id} className="rounded-2xl border border-white/10 bg-[#0b1527] p-5"><div className="flex justify-between gap-3"><span className="text-xs font-bold uppercase tracking-wide text-blue-300">{resource.provider}</span><span className="rounded-full bg-white/5 px-2 py-1 text-xs capitalize text-slate-300">{resource.pricing}</span></div><h3 className="mt-3 text-lg font-black">{resource.title}</h3><p className="mt-2 text-sm leading-6 text-slate-400">{resource.description}</p><div className="mt-3 flex flex-wrap gap-2">{matchedSkills.map((skill) => <span key={skill} className="rounded-full bg-emerald-300/10 px-2 py-1 text-xs text-emerald-200">{skill}</span>)}</div><p className="mt-4 text-xs text-slate-500"><strong className="text-slate-300">Why SEKUR recommends this:</strong> {reason}</p><a href={resource.affiliateUrl ?? resource.url} target="_blank" rel="noopener noreferrer" className="mt-5 inline-block font-bold text-blue-300">Open resource →</a>{(resource.sponsored || resource.affiliateUrl) && <p className="mt-2 text-xs text-slate-500">Sponsored or affiliate link</p>}</article>)}</div>;
}
